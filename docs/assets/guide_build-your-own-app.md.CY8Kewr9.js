import{_ as s,o as a,c as i,ae as o}from"./chunks/framework.BIONGpTb.js";const g=JSON.parse('{"title":"Build your own app","description":"","frontmatter":{},"headers":[],"relativePath":"guide/build-your-own-app.md","filePath":"guide/build-your-own-app.md"}'),t={name:"guide/build-your-own-app.md"};function n(p,e,l,d,r,c){return a(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="build-your-own-app" tabindex="-1">Build your own app <a class="header-anchor" href="#build-your-own-app" aria-label="Permalink to &quot;Build your own app&quot;">​</a></h1><h2 id="recommended-scaffold-with-create-app" tabindex="-1">Recommended: scaffold with create-app <a class="header-anchor" href="#recommended-scaffold-with-create-app" aria-label="Permalink to &quot;Recommended: scaffold with create-app&quot;">​</a></h2><p>The easiest way to start is to scaffold a new app with the official CLI. You get a monorepo with an app package and an example extension.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> create</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> @eclipse-docks/app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-app</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cd</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> my-app</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">npm</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dev</span></span></code></pre></div><p>This creates a project with:</p><ul><li><strong><code>packages/app</code></strong> — The Docks app (entry point, extension list, logo). Edit <code>packages/app/src/main.ts</code> to add or remove extensions and customize the toolbar.</li><li><strong><code>packages/example-extension</code></strong> — A sample extension that adds a view to the left sidebar (same tab bar as the Workspace / file browser). Use it as a reference for your own extensions.</li></ul><h3 id="customizing-the-logo" tabindex="-1">Customizing the logo <a class="header-anchor" href="#customizing-the-logo" aria-label="Permalink to &quot;Customizing the logo&quot;">​</a></h3><p>The main toolbar uses <strong><code>public/logo.svg</code></strong>. The startup splash uses <strong><code>public/logo-loading.svg</code></strong> by default (injected at build time by <code>appSplashPlugin()</code> from <code>@eclipse-docks/core/vite-plugin-app-splash</code>, wired in the app’s <code>vite.config.ts</code>). Replace those files to brand the app, or pass <strong><code>logo: { src, alt?, width?, height? }</code></strong> for a different asset or size. Pass <strong><code>description</code></strong> for a short tagline under the logo if you want one.</p><h3 id="splash-screen" tabindex="-1">Splash screen <a class="header-anchor" href="#splash-screen" aria-label="Permalink to &quot;Splash screen&quot;">​</a></h3><p>The HTML for the loading overlay (progress text, <code>app-load-progress</code> / <code>app-loaded</code> listeners, and keyframes) comes from core via <strong><code>appSplashPlugin()</code></strong>, not from a long copy-paste in <code>index.html</code>. Keep a minimal <code>index.html</code> with <code>&lt;div id=&quot;app-root&quot;&gt;&lt;/div&gt;</code> and add the plugin next to <code>resolveDepVersionsPlugin()</code> and <code>localAliasesPlugin()</code>. To fully customize the overlay, omit the plugin and provide your own markup, or keep a manual <code>#app-loading-overlay</code> block—the plugin skips injection when that id is already present.</p><p>From the new project root: <code>npm run dev</code> starts the app, <code>npm run build</code> builds it, <code>npm run preview</code> previews the production build. See the generated <code>README.md</code> in the project for a short overview.</p><h2 id="alternative-manual-setup-in-the-monorepo" tabindex="-1">Alternative: manual setup in the monorepo <a class="header-anchor" href="#alternative-manual-setup-in-the-monorepo" aria-label="Permalink to &quot;Alternative: manual setup in the monorepo&quot;">​</a></h2><p>If you prefer to work inside the Eclipse Docks repo or copy the default app by hand:</p><ol><li><p><strong>Create or copy the app package</strong><br> Add a new package under <code>packages/</code> (e.g. <code>packages/my-app</code>) or duplicate <code>packages/app</code>. Ensure the root <code>package.json</code> <code>workspaces</code> includes <code>packages/*</code>.</p></li><li><p><strong>Install and wire the app</strong><br> In the app package, depend on <code>@eclipse-docks/core</code> and the extensions you need (<code>@eclipse-docks/extension-*</code>).</p></li><li><p><strong>Entry point</strong><br> In your app entry (e.g. <code>main.ts</code>):</p><ul><li>Import the extensions you need (side-effect imports so they register).</li><li>Call <code>appLoaderService.registerApp(appDefinition, { autoStart: true, hostConfig: true })</code>. Add <code>marketplaceCatalogUrls</code> and optionally <code>dependencies</code> to the app definition if you use the marketplace or want to show dependency versions in About.</li></ul></li><li><p><strong>App definition</strong><br> Minimal example (default layout is <code>standard</code>, i.e. IDE):</p></li></ol><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { appLoaderService } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@eclipse-docks/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">appLoaderService.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">registerApp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    extensions: [</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;@eclipse-docks/extension-command-palette&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;@eclipse-docks/extension-settings-tree&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      &#39;@eclipse-docks/extension-ai-system&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    layout: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;standard&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { autoStart: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, hostConfig: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Use <code>layout: { id: &#39;standard&#39;, props: { &#39;show-bottom-panel&#39;: true } }</code> to parameterize the layout (props are applied as attributes when rendering).</p><p>To add another layout (e.g. dashboard), register a <strong>LayoutContribution</strong> to the <code>SYSTEM_LAYOUTS</code> slot (<code>id</code>, <code>name</code>, <code>component</code>, optional <code>onShow</code>). Users switch between layouts via the toolbar layout switcher.</p><ol start="5"><li><strong>Run and build</strong><br> From the repo root: <code>npm run dev</code> (or run the app package&#39;s dev script). For production: <code>npm run build</code> then build the app package; output will be in the app&#39;s <code>dist/</code>.<br> For branding, add or replace <strong><code>public/logo.svg</code></strong> (toolbar) and <strong><code>public/logo-loading.svg</code></strong> (splash), and use <strong><code>appSplashPlugin()</code></strong> in Vite config unless you replace the splash HTML yourself.</li></ol><p>See <a href="/docs/concepts/apps.html">Concepts: Apps</a> for all <code>AppDefinition</code> options and layout registration.</p>`,19)])])}const k=s(t,[["render",n]]);export{g as __pageData,k as default};
