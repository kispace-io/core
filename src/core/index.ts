// Framework Entry Point
//
// This is the main entry point for the framework.
// Importing this module bootstraps the framework (initializes services, registers components).
//
// For framework API usage, apps should import from './api/index.ts' instead.
// This entry point only handles bootstrap.
//
// Usage:
//   import './core/index.ts'  // Bootstrap framework
//   import { appLoaderService, commandRegistry } from './core/api/index.ts'  // Use API

// Bootstrap the framework (initialize services, register components, etc.)
import './bootstrap';

